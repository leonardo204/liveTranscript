import Foundation

/// 자막 오버레이 창 (스펙 §5.3).
///
/// TODO(M3): borderless / clear / 최상위(.screenSaver) / 클릭통과 NSWindow,
/// collectionBehavior(canJoinAllSpaces, fullScreenAuxiliary)로 전체화면 위 표시,
/// SwiftUI Text + 스타일(외곽선/그림자/글로우) 렌더링.
final class OverlayWindow {
    // M0 스텁 — 구현은 M3.
}
