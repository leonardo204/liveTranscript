import Foundation

/// Gemini 3.5 Live Translate WebSocket 클라이언트 (스펙 §5.1).
///
/// TODO(M2): URLSessionWebSocketTask 연결, setup 메시지 전송,
/// 오디오 청크 송신, 수신 이벤트(번역/원문 transcription) 파싱,
/// 15분 세션 자동 재연결(스펙 §6).
final class GeminiLiveClient {
    // M0 스텁 — 인터페이스/구현은 M2.
}
